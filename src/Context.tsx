
import { createContext } from "react";

export const UserContext = createContext<any | null | undefined>(null);
export const PriceCinfigContext = createContext<any | null | undefined>(null);
export const InsuranceConfigContext = createContext<any | null | undefined>(null);

